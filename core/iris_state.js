// core/iris_state.js — Stato coscienziale (Step 4+)
// Riepilogo di mode, lingua, pesi, versione. Placeholder.
export function getStateSummary() {
  return {
    mode: "(pending)",
    language: "(pending)",
    version: "(pending)",
    weights: "(pending)"
  };
}
