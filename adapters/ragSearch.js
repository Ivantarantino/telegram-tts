// adapters/ragSearch.js — RAG Qdrant + fallback (Step 4)
// Placeholder per la ricerca vettoriale.
export async function ragSearch(/* query */) {
  throw new Error("Step 4: implementare ragSearch.js.");
}
