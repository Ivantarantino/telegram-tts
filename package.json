{
  "name": "telegram-tts",
  "version": "1.0.0",
  "type": "module",  // Cruciale per ES6 import (non "commonjs")
  "scripts": {
    "start": "node index.js"  // O il nome del tuo file principale
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0",
    "express": "^4.18.0",
    "node-fetch": "^2.7.0",
    "google-tts-api": "^2.2.3"
  },
  "engines": {
    "node": ">=18"  // Specifica Node.js >=18 per compatibilità
  }
}
